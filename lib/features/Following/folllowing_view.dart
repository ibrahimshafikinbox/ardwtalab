import 'package:flutter/material.dart';

class Following_View extends StatelessWidget {
  const Following_View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Following"),
    );
  }
}
